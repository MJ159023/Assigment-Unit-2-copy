/*
 * The program runs Hare class
 *
 * @author  Michael Bruneau
 * @version 1.0
 * @since   2020-11-30
 */

public class Hare extends Animal {
    /**
     * The Squirell's climb distance
     */
    private int climbDistance;

    /**
     * sets default values for animals.
     *
     * @param setColor
     * @param setName
     * @param setSpeed
     * @param setHealth
     * @param setDefense
     * @param setAttackPower
     * @param setAttackAccuracy
     * @param setAnimalElvation
     * @param setFallDistance
     * @param setClimbDistance
     */
    public Hare(final String setColor, final String setName,
                    final int setSpeed, final int setHealth,
                    final int setDefense, final int setAttackPower,
                    final int setAttackAccuracy, final int setAnimalElvation,
                    final int setFallDistance, final int setClimbDistance) {
        super(setColor, setName, setSpeed, setHealth, setDefense,
                        setAttackPower, setAttackAccuracy, setAnimalElvation,
                        setFallDistance);
        this.slipSpeed = setSlipSpeed;
        this.hopHeight = setHopHeight;
        this.climbDistance = setClimbDistance;
    }

    /**
     * Squirell slow down.
     *
     * @param brakePower
     * @param brakeTime
     * @return speed
     */
    public int brake(final int brakePower, final int brakeTime) {
       setSpeed(getSpeed() - (brakePower * brakeTime) + this.slipSpeed);
       setDistanceTravelled(getSpeed() * brakeTime);
       System.out.println(getName() + " begins to try to hault traveling "
                       + getDistanceTravelled() + "ft.");
       setDistanceTravelled(0);
       return getSpeed();
    }

    /**
     * Squirell speeds up. 
     *
     * @param accelerationPower
     * @param accelerationTime
     * @return speed
     */
    public int accelerate(final int accelerationPower,
                    final int accelerationTime) {
        setSpeed(getSpeed() + (accelerationPower * accelerationTime)
                        + this.slipSpeed);
        setDistanceTravelled(getSpeed() * accelerationTime);
        System.out.println(getName() + " begins hop and travels "
                        + getDistanceTravelled() + "ft.");
        setDistanceTravelled(0);
        return getSpeed();
    }

    /**
     * the Squirell's special movemnet ability is it can hop super high and far.
     *
     * @return height
     */
    public int specialMovement() {
        setHeight(getHeight() +  hopHeight);
        System.out.println("The Hare begins lower itself"
                        + "to ground readying itself.\nThen in instant"
                        +" the Hare hops" + getHeight() + "ft into the air.");
        return getHeight();
    }

    /**
     * the Squirell's special ability is it can bounce kick off enemies.
     *
     * @param enemyAttacked
     * @param sepecialDamage
     * @return damage
     */
    public int specialAbility(final String enemyAttacked,
                    final int specialDamage) {
        setHeight(getHeight() - getFallDistance());
        setFallCounter(0);
        System.out.println("Hare falls " + getFallDistance() + 
                        "ft as it bounces off " + enemyAttacked);
        setDamage(specialDamage);
        return getDamage();
    }

    
    /**
     * Squirell's climbing speed.
     *
     * @return height
     */
    public int climb() {
        setHeight(getHeight() + climbDistance);
        System.out.println("The Hare begins scartching the the trunk "
                        + "tree latching on with it's paws.\nIt slowly climbs "
                        + getHeight() + "ft up the tree");
        return getHeight();
    }
}
